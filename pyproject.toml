[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "core_clients"
version = "0.1.0"
description = "Generic automation clients for internal projects"
requires-python = ">=3.10"
dependencies = []
